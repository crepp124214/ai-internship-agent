# Tracker Agent configuration

name: "tracker_agent"
description: "Responsible for advising the next best steps for job applications."
enabled: true

llm:
  provider: "openai"
  model: "gpt-4"
  temperature: 0.5
  max_tokens: 1000
