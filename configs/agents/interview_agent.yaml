# Interview Agent configuration

name: "interview_agent"
description: "Responsible for generating interview questions and evaluating answers."
enabled: true

llm:
  provider: "minimax"
  model: "abab6.5s-chat"
  temperature: 0.5
  max_tokens: 1200
  base_url: "https://api.minimax.chat/v1"
